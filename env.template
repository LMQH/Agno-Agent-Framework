# ============================================================================
# Agno Backend API Service 环境配置模板
# ============================================================================
# 使用方法：
# 1. 复制此文件为 .env
# 2. 根据实际情况修改配置值
# 3. 不要将 .env 文件提交到版本控制系统
# ============================================================================

# ============================================================================
# 应用基础配置
# ============================================================================
# 运行环境: development | production | testing
APP_ENV=development

# 服务监听地址
APP_HOST=0.0.0.0

# 服务监听端口
APP_PORT=8564

# ============================================================================
# MySQL 数据库配置 - Agno专用数据库
# ============================================================================
# Agno专用数据库主机地址
AGNO_MYSQL_HOST=localhost

# Agno专用数据库端口
AGNO_MYSQL_PORT=3306

# Agno专用数据库用户名
AGNO_MYSQL_USER=root

# Agno专用数据库密码
AGNO_MYSQL_PASSWORD=password

# Agno专用数据库名称（db_schema）
AGNO_MYSQL_DB_SCHEMA=agno_backend

# ============================================================================
# Agno 数据库表名配置
# ============================================================================
# Workflow会话表名
WORKFLOW_SESSION_TABLE=workflow_sessions

# Team会话表名
TEAM_SESSION_TABLE=team_sessions

# Agent会话表名
AGENT_SESSION_TABLE=agent_sessions

# 记忆表名
AGNO_MEMORY_TABLE=agno_tags_memories

# 追踪表名
AGNO_TRACES_TABLE=agno_tags_traces

# Span表名
AGNO_SPANS_TABLE=agno_tags_spans

# 指标表名
AGNO_METRICS_TABLE=agno_tags_metrics

# 评估表名
AGNO_EVAL_TABLE=agno_tags_evaluations

# 知识表名
AGNO_KNOWLEDGE_TABLE=agno_tags_knowledge

# 文化知识表名
AGNO_CULTURE_TABLE=agno_tags_culture

# ============================================================================
# MySQL 数据库配置 - 业务专用数据库
# ============================================================================
# 业务数据库主机地址
BUSINESS_MYSQL_HOST=localhost

# 业务数据库端口
BUSINESS_MYSQL_PORT=3306

# 业务数据库用户名
BUSINESS_MYSQL_USER=root

# 业务数据库密码
BUSINESS_MYSQL_PASSWORD=password

# 业务数据库列表（支持多个数据库，用逗号分隔）
# 可以配置任意数量的数据库，例如: BUSINESS_MYSQL_DATABASES=db1,db2,db3
# 所有数据库共享相同的连接配置（HOST、PORT、USER、PASSWORD）
BUSINESS_MYSQL_DATABASES=

# ============================================================================
# 数据库连接池配置（通用）
# ============================================================================
# 数据库连接池大小（可选，默认20）
MYSQL_POOL_SIZE=20

# 数据库连接回收时间，单位：秒（可选，默认3600）
MYSQL_POOL_RECYCLE=3600

# 是否输出SQL日志（可选，默认false）
MYSQL_ECHO=false

# ============================================================================
# Milvus 向量数据库配置
# ============================================================================
# Milvus 服务器主机地址
MILVUS_HOST=localhost

# Milvus 服务器端口
MILVUS_PORT=19530

# Milvus 数据库名称（可选，默认使用 "default"）
MILVUS_DATABASE=default

# Milvus 用户名（可选，如果 Milvus 启用了认证）
# MILVUS_USER=root

# Milvus 密码（可选，如果 Milvus 启用了认证）
# MILVUS_PASSWORD=password

# ============================================================================
# 模型API配置
# ============================================================================
# 主模型API地址（DeepSeek私有部署）
MODEL_API_BASE_URL=https://api.modelarts-maas.com/v1

# 主模型API密钥
MODEL_API_KEY=your_model_api_key_here

# 主模型名称
MODEL_NAME=deepseek-v3.2-exp

# 嵌入模型API地址（阿里云DashScope）
EMBEDDING_API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 嵌入模型API密钥
EMBEDDING_API_KEY=your_embedding_api_key_here

# 嵌入模型名称（可选，默认text-embedding-v2）
EMBEDDING_MODEL_NAME=text-embedding-v2

# ============================================================================
# 日志配置
# ============================================================================
# 日志级别: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# AgentOS 配置
# ============================================================================
# 是否启用追踪（可选，默认true）
AGENTOS_TRACING=true

# ============================================================================
# 安全配置（生产环境建议配置）
# ============================================================================
# CORS允许的源（多个用逗号分隔，生产环境请修改）
# CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# API密钥（可选，用于API认证）
# API_SECRET_KEY=your_secret_key_here
